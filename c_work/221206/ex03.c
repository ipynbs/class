#include <stdio.h>
// tab ->
// shift + tab <-
// alt + shift + f 자동정렬
int main()
{
    // 10 + 20
    // 변수 num 에다가 넣어서
    // 출력해보기
    int a = 10;
    int b = 20;
    int num = a + b;
    int c= 30;
    int d = 40;
    int e = 50;

    printf("num = %d", num);
    return 0;
}